{
  "name": "pantry",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "docker compose up -d && turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.3.0",
    "prettier": "^3.8.1",
    "turbo": "^2.8.12",
    "typescript": "5.9.2",
    "eslint": "^9.39.1",
    "@pantry/eslint-config": "workspace:*"
  },
  "lint-staged": {
    "**/*.{ts,tsx,md,js,jsx,json,css,scss,html}": [
      "bunx prettier --write"
    ],
    "**/*.{ts,tsx,js,jsx}": [
      "bunx eslint --fix"
    ]
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.3.4",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
